import 'package:flutter/material.dart';

class HomeProfesorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Regitro Pedagógico'),
      ),
    );
  }
}
